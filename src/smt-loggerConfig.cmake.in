include(CMakeFindDependencyMacro)

#  you can do as below to get some value during configuration,
# set(my-config-var @my-config-var@)

# just like find_package() to find dependencies
# find_dependency(MYDEP REQUIRED)

# Add the targets file
include("${CMAKE_CURRENT_LIST_DIR}/smt-loggerTargets.cmake")